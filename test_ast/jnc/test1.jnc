test ()
{
	printf ("test ()\n");
}

//.............................................................................

property
{
	int 
	get ()
	{	
		printf ("MyProp.get ()\n");
	}

	set (char x)
	{	
		printf ("MyProp.set (char x = %d)\n", x);
	}

	set (long x)
	{	
		printf ("MyProp.set (long x = %d)\n", x);
	}

} MyProp;

//.............................................................................

int
printf (
	char* unsafe pFormat,
	unsafe ...
	);

void
PointerCheck (int* p);

//.............................................................................

test (int a)
{
	printf ("test (int x = %d)\n", a);
}

test (double a)
{
	printf ("test (double x = %f)\n", a);
}

test (
	int x, 
	int y
	)
{
	printf ("test (int x = %d, int y = %d)\n", x, y);
}

int
main ()
{
	printf ("hui\n");

	test (10, 20);

	int x, y;

	MyProp = (long) x;

	printf ("govno i muravei\n");
	return 0;
}

//.............................................................................
