//.............................................................................

property MyProp
{
	int propvalue;

	set (int x)
	{
		printf ("MyProp.set (%d)\n", x);
		propvalue = x;
		foo ();
	}

	foo ()
	{
		printf ("MyProp.foo (%d)\n", propvalue);
	}
};

int 
main ()
{
	printf ("hui govno i muravei\n");

	MyProp = 50;

	int x = MyProp;
	printf ("x = %d\n", x);	

	return 0;
}

//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................