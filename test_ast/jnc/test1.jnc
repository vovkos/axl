//.............................................................................

int g;

int 
main ()
{
	printf ("main ()\n");

	int property* py = &g;

	*py = 200;
	printf ("g = %d\n", g);

	int x = *py;
	printf ("x = %d\n", x);

	return 0;
}
 
//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

int
GetCurrentThreadId ();

bool
CreateThread (function* pfn ());

//.............................................................................
