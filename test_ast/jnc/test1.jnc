//.............................................................................

foo (int x)
{
	printf ("foo (%d)\n", x);
}

function bar (int x)
{
	printf ("bar (%d)\n", x);
}

int 
main ()
{
	printf ("hui govno i muravei\n");
	
	thin function* pfn (int) = foo;
	pfn (500);

/*	thin multicast mc (int);
	mc += &foo;
	mc += &bar; */

	return 0;
}

//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................