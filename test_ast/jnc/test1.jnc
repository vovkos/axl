//.............................................................................


int 
indexed property MyProp (
	int i,
	int j
	);

int
MyProp.get (
	int i, 
	int j
	)
{
	printf ("MyProp.get (%d, %d)\n", i, j);
	return 100;
}

MyProp.set (
	int i,
	int j,
	int x
	)
{
	printf ("MyProp.set (%d, %d, %d)\n", i, j, x);
}


int 
main ()
{
	printf ("hui govno i muravei\n");

	int indexed property* pr (int) = MyProp [50];

	int x = *pr [55];

	*pr [56] = 200;

	return 0;
}

//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................