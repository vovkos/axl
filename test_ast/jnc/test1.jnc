//.............................................................................

int
foo (
	char x, 
	short y,
	long z
	)
{
	return printf ("foo (%d, %d, %d)\n", x, y, z);
}

int
main ()
{
	printf ("hui govno i muravei\n");

	void function* fn (char) = foo ~(, 1, 2);

	fn (100);
	return 0;
}

//.............................................................................

int
printf (
	unsafe char* pFormat,
	unsafe ...
	);

//.............................................................................