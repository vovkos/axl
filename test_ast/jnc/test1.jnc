//.............................................................................

foo (object o)
{
	object <int> a = o;
	printf ("foo (%d)\n", a.value);
}

int 
main ()
{
	printf ("main ()\n");

	object <int> a = new object <int>;
	a.value = 100;

	foo (a);

	return 0;
}

//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................
