//.............................................................................

class CTest
{
public:
	mutable int m_Value;

	Foo (int x) const
	{
		printf ("CTest.Foo (x = %d)\n", x);
		m_Value = x;
	}

	Bar ()
	{
		printf ("CTest.Bar () { m_v = %d }\n", m_Value);
	}
};


/*

class CTest
{
	int m_x;

	void
	Foo (int x)
	{
		printf ("CTest.Foo (x = %d)\n", x);
		m_x = x;
	}

	void
	Bar ()
	{
		printf ("CTest.Bar () { m_x = %d }\n", m_x);
	}
};

*/

int
main ()
{
	printf ("hui govno i muravei\n");

	CTest t = stack new CTest;

	t.Foo (100);
	t.Bar ();

	return 0;
}

//.............................................................................

int
printf (
	unsafe char* pFormat,
	unsafe ...
	);

//.............................................................................