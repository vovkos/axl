//.............................................................................

int autoget bindable property MyProp;

MyProp.set (int x)
{
	printf ("CTest.MyProp.set (%d)\n", x);
	propvalue = x;
}

foo ()
{
	printf ("foo ()\n");
}

bar ()
{
	printf ("bar ()\n");
}

int 
main ()
{
	printf ("hui govno i muravei\n");

	MyProp.onchange += foo;
	MyProp.onchange += bar;

	MyProp = 50;

	int x = MyProp;
	printf ("x = %d\n", x);	

	return 0;
}

//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................