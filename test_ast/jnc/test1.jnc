//. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .

struct TPoint
{
	int m_x;
	int m_y;

	property MyProp
	{
		int
		get ()
		{
			printf ("TPoint.MyProp.get ()\n");
			return m_x * 10 + m_y;
		}
		
		set (int z)
		{
			printf ("TPoint.MyProp.set (%d)\n", z);
			m_x = z / 10;
			m_y = z % 10;
		}
	};

	Setup (
		int x,
		int y
		)
	{
		printf ("TPoint.Setup (%d, %d)\n", x, y);	

		m_x = x;
		m_y = y;
	}
};

//. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .

int 
main ()
{
	printf ("struct / union members\n");

	TPoint p;
	p.MyProp = (34);

	printf ("{ %d, %d }\n", p.m_x, p.m_y);

	p.Setup (10, 20);
	printf ("{ %d, %d }\n", p.m_x, p.m_y);

	return 0; 
}
 
//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................
