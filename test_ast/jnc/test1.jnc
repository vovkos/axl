//.............................................................................

int
bar ()
{
	printf ("bar ()\n");
	return 10;
}

foo ()
{
	printf ("foo ()\n");

	static int x = bar ();
	printf ("x = %d\n", x);
}

int 
main ()
{
	printf ("once & static new test\n");

	for (int i = 0; i < 5; i++)
	{
		printf ("i = %d\n", i);

		once printf ("once stmt\n");

		foo ();
	}

	return 0; 
}

//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................
 