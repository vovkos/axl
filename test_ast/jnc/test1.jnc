//.............................................................................

bindable int Red, Green, Blue;

int autoget property Yellow, Purple, Cyan;

Yellow.set (int Value)
{
	propvalue = Value;
	printf ("Yellow.set (%d)\n", Value);
}

Purple.set (int Value)
{
	propvalue = Value;
	printf ("Purple.set (%d)\n", Value);
}

Cyan.set (int Value)
{
	propvalue = Value;
	printf ("Cyan.set (%d)\n", Value);
}

autoev AColor
{
	Purple = Red + Blue;
	Yellow = Red + Green;
	Cyan = Green + Blue;
};

int 
main ()
{
	printf ("hui govno i muravei\n");

	Red   = 10;
	Green = 100;
	Blue  = 1000; 

	stack new AColor;

	Red   = 20;
	Green = 200;
	Blue  = 2000; 

	return 0;
}

//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................