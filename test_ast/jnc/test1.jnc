//.............................................................................

int autoget property IntProp;

IntProp.set (int x)
{
	printf ("IntProp.set (%d)\n", x);
	propvalue = x;
}

int 
main ()
{
	printf ("main ()\n");

	int property* py = &IntProp;

	*py = 200;

	int x = *py;
	printf ("x = %d\n", x);

	return 0;
}
 
//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

int
GetCurrentThreadId ();

bool
CreateThread (function* pfn ());

//.............................................................................
