//.............................................................................

int autoget property MyProp;

MyProp.set (int x)
{
	printf ("MyProp.set (%d)\n", x);
	propvalue = x;
}

int 
main ()
{
	printf ("jancy property test\n");

	MyProp = 100;

	int x = MyProp;
	printf ("x = %d\n", x);

	return 0;
}
 
//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................
