//. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .

enumc EProto: short
{
	EProto_Icmp = 1,
	EProto_Tcp  = 6,
	EProto_Udp  = 17,
};

//. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .

/*
class C1
{
	static int s_Field;

	static this ()
	{
		printf ("C1.static this ()\n");
	}

	static ~this ()
	{
		printf ("C1.static ~this ()\n");
	}

	this ()
	{
		printf ("C1.this ()\n");
	}

	~this ()
	{
		printf ("C1.~this ()\n");
	}

	static ~this ()
	{
		printf ("C1.static ~this ()\n");
	}

	static 
	int 
	foo ()
	{
		printf ("C1.foo ()\n");
		return 20;
	}
}
*/

foo (
	int a = 1,
	int b = 2,
	int c = 3,
	int d = 4
	)
{
	printf ("foo (%d, %d, %d, %d)\n", a, b, c, d);
}

int 
main ()
{
	printf ("main ()\n");

	EProto Proto;
	printf ("sizeof (Proto) = %d\n", sizeof (EProto_Tcp));

	return 0;
}

//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................
