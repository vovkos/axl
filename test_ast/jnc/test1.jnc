//.............................................................................

int indexed property MyProp (int i);

int
MyProp.get (int i)
{
	printf ("MyProp.get (%d)\n", i);
	return 100;
}

MyProp.set (
	int i,
	int x
	)
{
	printf ("MyProp.set (%d, %d)\n", i, x);
}

int 
main ()
{
	printf ("hui govno i muravei\n");

	int thin indexed property* pr (int) = MyProp;

	int x = *(pr [5]);

	*(pr [10]) = 200;

	return 0;
}

//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................