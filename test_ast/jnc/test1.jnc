//.............................................................................

int
printf (
	char* unsafe pFormat,
	unsafe ...
	);


//.............................................................................

int g_Baud;
int g_Baud2;

bindable property 
{
	int
	get ()
	{
		printf ("PBaud.get () = %d\n", g_Baud);
		return g_Baud;
	}

	set (int Value)
	{
		printf ("PBaud.set (%d)\n", Value);
		g_Baud = Value;
	}
} PBaud;

//.............................................................................

OnBaudChange ()
{
	printf ("Baud has changed to %d!\n", PBaud);
}

int
main ()
{
	printf ("hui\n");
	
	onchange (PBaud) += OnBaudChange;

	printf ("i muravei\n");
	return 0;
}

//.............................................................................
