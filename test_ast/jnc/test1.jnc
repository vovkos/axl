//.............................................................................

int
printf (
	char* unsafe pFormat,
	unsafe ...
	);

void
PointerCheck (int* p);

//.............................................................................

void
test ()
{
	printf ("test ()\n");
}

void 
test (int a)
{
	printf ("test (int x = %d)\n", a);
}

void 
test (double a)
{
	printf ("test (double x = %f)\n", a);
}

void 
test (int x, int y)
{
	printf ("test (int x = %d, int y = %d)\n", x, y);
}

int
main ()
{
	printf ("hui\n");

	double x;
	int y;
	
	x = (char) y;

	test (x);


	printf ("govno i muravei\n");
	return 0;
}

//.............................................................................
