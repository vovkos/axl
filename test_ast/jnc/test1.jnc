//. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .

/*

foo (
	int a = 1,
	int b = 2,
	int c = 3,
	int d = 4
	)
{
	printf ("foo (%d, %d, %d, %d)\n", a, b, c, d);
}

int 
main ()
{
	printf ("main ()\n");

	function* pfn (
		int a = 10,
		int b = 20,
		int c = 30,
		int d = 40
		);

	return 0;
} */

int
main ()
{
	int a [3] [3] = 
	{
		{ 1,  2,  3 },
		{ 4, -5,  6 },
		{ 7,  8,  9 },
	};

	int i, j;

	for (i = 0; i < 3; i++)
	for (j = 0; j < 3; j++)
		if (a [i] [j] < 0)
			break 2;

	printf ("%d\n", a [i] [j]);  // -5

	return 0;
}

//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................
