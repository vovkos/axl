//.............................................................................

int
printf (
	char* unsafe pFormat,
	unsafe ...
	);

void
PointerCheck (int* p);

//.............................................................................

void
test ()
{
	printf ("test ()\n");
}

void 
test (int a)
{
	printf ("test (int x = %d)\n", a);
}

void 
test (double a)
{
	printf ("test (double x = %f)\n", a);
}

void 
test (char x, short y)
{
	printf ("test (int x = %d, int y = %d)\n", x, y);
}

property
{
	int 
	get ()
	{	
		printf ("MyProp.get ()\n");
	}

	void
	set (char x)
	{	
		printf ("MyProp.set (char x = %d)\n", x);
	}

	void
	set (short x)
	{	
		printf ("MyProp.set (short x = %d)\n", x);
	}
} MyProp;

int
main ()
{
	printf ("hui\n");

	int x, y;

	MyProp = (long) x;

	printf ("govno i muravei\n");
	return 0;
}

//.............................................................................
