//.............................................................................

/*

int property MyIntProp;

int
MyIntProp.get ()
{
	printf ("MyIntProp.get ()\n");
	return 100;
}

MyIntProp.set (int x)
{
	printf ("MyIntProp.set (%d)\n", x);
}

*/

foo (int x)
{
	printf ("foo (%d)\n", x);
}

foo (double x)
{
	printf ("foo (%f)\n", x);
}

int
main ()
{
	printf ("hui govno i muravei\n");
	foo (100);
	foo ((double) 200);
//	MyIntProp = 100;
	return 0;
}

//.............................................................................

int
printf (
	unsafe char* pFormat,
	unsafe ...
	);

//.............................................................................