//. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .

/*

foo (
	int a = 1,
	int b = 2,
	int c = 3,
	int d = 4
	)
{
	printf ("foo (%d, %d, %d, %d)\n", a, b, c, d);
}

int 
main ()
{
	printf ("main ()\n");

	function* pfn (
		int a = 10,
		int b = 20,
		int c = 30,
		int d = 40
		);

	return 0;
} */

struct TPoint
{
	int x;
	int y;
	int z = 30;

	this ()
	{
		x = 10;
		y = 20;
	}
};

int
main ()
{
	TPoint Point;

	printf ("Point = { %d, %d, %d }\n", Point.x, Point.y, Point.z);

	return 0;
}

//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................
