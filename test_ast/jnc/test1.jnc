//.............................................................................

int
printf (
	char* unsafe pFormat,
	unsafe ...
	);


//.............................................................................
/*
int g_Baud;
int g_Baud2;

bindable property 
{
	int
	get ()
	{
		printf ("PBaud.get () = %d\n", g_Baud);
		return g_Baud;
	}

	set (int Value)
	{
		printf ("PBaud.set (%d)\n", Value);
		g_Baud = Value;
	}
} PBaud;

//.............................................................................

autoev ASerialRule
{
	printf ("autoev ASerial: Baud has changed to %d!\n", PBaud);
};
*/

class CTest
{
	preconstruct ()
	{
		printf ("CTest.preconstruct\n");
	}

	~this ()
	{
		printf ("CTest.~this\n");
	}
};

int
main ()
{
	printf ("hui\n");

	stack new CTest;

//	ASerialRule SerialRule = stack new ASerialRule;
//	PBaud = 100;

	printf ("i muravei\n");
	return 0;
}

//.............................................................................
