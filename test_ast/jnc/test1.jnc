int
printf (
	char* unsafe pFormat,
	unsafe ...
	);


//.............................................................................

void
PointerCheck (int* p);

//.............................................................................

test (
	int x, 
	int y
	)
{
	printf ("test (int x = %d, int y = %d)\n", x, y);
}

test2 (
	int x, 
	int y
	)
{
	printf ("test2 (int x = %d, int y = %d)\n", x, y);
}

//.............................................................................

int
main ()
{
	printf ("hui\n");

	int x;		

	event
	OnMouseClick (
		int x, 
		int y
		);

	OnMouseClick = test;
	OnMouseClick += test2;

	OnMouseClick (10, 20);

	printf ("govno\n");

	OnMouseClick -= test;

	OnMouseClick (30, 40);

	int y;		

	printf ("i muravei\n");
	return 0;
}

//.............................................................................
