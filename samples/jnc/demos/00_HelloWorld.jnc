// the simpliest possible program in jancy
// at this level jancy looks exactly like C/C++

//.............................................................................

// entry point

int main ()
{
	printf ("hello world!\n");

	const char* p = "bzzz";

	int64 x = strlen (p);
	printf ("x = %d\n", x);
	
	return 0;
}

//.............................................................................
