//.............................................................................

int property gx_MyProp;

int 
gx_MyProp.get ()
{
	printf ("gx_MyProp.get ()\n");
	return 100;
}

gx_MyProp.set (int x)
{
	printf ("gx_MyProp.set (%d)\n", x);
}

int  
main ()
{
	printf ("main ()\n");

	property 
	{
		int 
		get ();

		set (int);
	} thin* px = &gx_MyProp;

	int x = *px;
	*px = 500;
	

	return 10;
}

//.............................................................................

// system functions

int
printf (
	unsafe thin const char* pFormat,
	unsafe ...
	);

void
RunGc ();

//.............................................................................
