//.............................................................................

thread int t_TestInt;

class C1
{
	construct ()
	{
		printf ("C1.construct () { t_TestInt = %d }\n", t_TestInt);
	}

	destruct ()
	{
		printf ("C1.destruct () { t_TestInt = %d }\n", t_TestInt);	
	}
}

int 
main ()
{
	printf ("main ()\n");

	t_TestInt = 100;

	C1 c;

	return 10; 
}

//.............................................................................

// system functions

int
printf (
	unsafe thin const char* pFormat,
	unsafe ...
	);

void
RunGc ();

int
GetCurrentThreadId ();

bool
CreateThread (function* pf ());

void
Sleep (unsigned int32 MsCount);

//.............................................................................
