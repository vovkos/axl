//.............................................................................
/*
class C1
{
	construct ()
	{
		printf ("C1.construct ()\n");
	}

	destruct ()
	{
		printf ("C1.destruct ()\n");
	}
}
*/

property gx_MyProp
{
	int m_x;

	int 
	get ()
	{
		printf ("gx_MyProp.get ()\n");
		return m_x;
	}

	set (int x)
	{
		printf ("gx_MyProp.set (%d)\n", x);
		m_x = x;
	}
}

int  
main ()
{
	printf ("main ()\n");

	gx_MyProp = 200;
	int x = gx_MyProp;
	printf ("x = %d\n", x);

	return 10;
}

/*

class C1
{
	int m_Int;

	autoev m_MyAutoEv ()
	{
		m_Int = gx_MyProp * 2;
	}
}

int bindable gx_MyProp;

int  
main ()
{
	printf ("main ()\n");

	C1 c;
	c.m_MyAutoEv.Start ();

	gx_MyProp.onchange.Add (foo);
	gx_MyProp.onchange.Add (bar ~(5));

	gx_MyProp = 12;

	printf ("c.m_Int = %d\n", c.m_Int);

	return 10;
}

foo ()
{
	printf ("foo ()\n");
}

bar (int x)
{
	printf ("bar (%d)\n", x);
}

*/

//.............................................................................

// system functions

int
printf (
	unsafe thin const char* pFormat,
	unsafe ...
	);

void
RunGc ();

//.............................................................................
