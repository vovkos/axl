//.............................................................................

struct TPoint
{
	int m_x;
	int m_y;
}

thread TPoint t_Point;
// thread int t_Int = 200;
	
int 
main ()
{
	printf ("main ()\n");

	thread int x = 100;

	TPoint Point = { 5, 6 };

	printf ("Point = { %d, %d }\n", Point.m_x, Point.m_y);

	return 10; 
}

//.............................................................................

// system functions

int
printf (
	unsafe const thin char* pFormat,
	unsafe ...
	);

//.............................................................................
