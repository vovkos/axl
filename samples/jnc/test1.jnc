//.............................................................................

class C1
{
	int
	operator += (int x)
	{
		printf ("operator += (%d)\n");		
		return 100;
	}

	operator () (int x)
	{
		printf ("operator (int %d)\n", x);		
	}

	operator () (double x)
	{
		printf ("operator (double %f)\n", x);		
	}
}


int  
main ()
{
	printf ("main ()\n");

	C1 c;
	c (5.5);	

	return 10;
}

//.............................................................................

// system functions

int
printf (
	unsafe thin const char* pFormat,
	unsafe ...
	);

void
RunGc ();

//.............................................................................
