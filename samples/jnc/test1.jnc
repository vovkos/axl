//.............................................................................

struct TPoint
{
	int m_x;
	int m_y;

	preconstruct ()
	{
		printf ("TPoint.preconstruct ()\n");
	}

	construct ()
	{
		printf ("TPoint.construct ()\n");
	}

	static construct ()
	{
		printf ("TPoint.static construct ()\n");
	}

	static destruct ()
	{
		printf ("TPoint.static destruct ()\n");
	}
}

int  
main ()
{
	printf ("main ()\n");

	foo ();

	return 10;
}

foo ()
{
	TPoint Point;
}

//.............................................................................

// system functions

int
printf (
	unsafe thin const char* pFormat,
	unsafe ...
	);

void
RunGc ();

//.............................................................................
