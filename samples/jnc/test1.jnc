//.............................................................................

int  
main ()
{
	printf ("main ()\n");

	thin multicast m (int);
	m += foo;
	m += bar;	
	m = zoo;
	m (100);	

	return 10;
}

foo (int x)
{
	printf ("foo (%d)\n", x);
}

bar (int x)
{
	printf ("bar (%d)\n", x);
}

zoo (int x)
{
	printf ("zoo (%d)\n", x);
}

//.............................................................................

// system functions

int
printf (
	unsafe thin const char* pFormat,
	unsafe ...
	);

void
RunGc ();

//.............................................................................
