// mcjit-dwarf line info test

//.............................................................................

struct Point
{
	int m_x;
	int m_y;
}

class C
{
	bool pitcher foo (Point const* point)
	{
		printf ("foo ()\n");
		return true;
	}
}

int main ()
{
	printf ("hello world!\n");

	stack Point point;

	C c;
	try c.foo (&point);
	try c.foo (&point);
	try c.foo (&point);
	try c.foo (&point);
	try c.foo (&point);
	try c.foo (&point);

	return 0;
}

Point* g_p;

//.............................................................................
